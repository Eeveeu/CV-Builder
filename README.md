# CV Builder — Конструктор резюме

Создавай и публикуй своё резюме онлайн. Без сложной регистрации, без подтверждений. Просто напиши, посмотри превью, скачай PDF или сохрани несколько вариантов.

## Что можно делать?

- 📝 **Написать резюме** — заполнить форму: личная информация, опыт работы, образование, навыки, языки, ссылки
- 📄 **Скачать PDF** — получить красиво отформатированный PDF с поддержкой кириллицы
- 📚 **Сохранить варианты** — хранить несколько вариантов резюме в личной библиотеке (SQLite база)
- 🎨 **Тёмная тема** — приятно для глаз
- ➕ **Добавлять/удалять разделы** — динамически добавлять опыт, образование, навыки, языки
- 📱 **Работает на мобильных** — адаптивный дизайн
- 🔒 **Твои данные остаются твоими** — всё обрабатывается локально

## Локальная установка

### Требования
- PHP 8.2+ (рекомендуется 8.3)
- Composer
- Git

### Установка

1. Клонируй репозиторий:
```bash
git clone https://github.com/Eeveeu/CV-Builder.git
cd cv-builder
```

2. Установи зависимости:
```bash
composer install
```

3. Скопируй конфиг:
```bash
cp .env.example .env
```

4. Создай необходимые директории:
```bash
mkdir -p storage/framework/{cache,sessions,views}
mkdir -p database
chmod -R 755 storage database
```

5. Запусти dev сервер:
```bash
php -S localhost:8000 -t public
```

6. Открой в браузере:
```
http://localhost:8000/cv
```

## Как использовать

1. **Заполни форму** — добавь личную информацию, опыт работы, образование, навыки, языки, ссылки
2. **Добавляй разделы** — нажми "➕ Добавить опыт" или "➕ Добавить образование" для новых пунктов
3. **Удаляй пункты** — каждый пункт имеет кнопку "❌ Удалить"
4. **Смотри превью** — нажми "👁️ Предпросмотр" чтобы увидеть как выглядит
5. **Скачай PDF** — нажми "⬇️ Скачать" чтобы сохранить файл resume.pdf
6. **Сохрани варианты** — нажми "📚 Сохранить в библиотеку" чтобы сохранить разные версии

## Развёртывание на продакшене

### Самый простой вариант: Render.com (Бесплатно!)

1. Запуши код на GitHub
2. Создай аккаунт на https://render.com
3. Подключи свой GitHub репозиторий
4. Разверни в 2 клика!

Полный гайд: смотри `RENDER_DEPLOYMENT.md`

Приложение будет доступно: `https://your-app.onrender.com/cv`

### Другие варианты

- **Heroku** — смотри `DEPLOYMENT.md`
- **VPS (Ubuntu/Debian)** — смотри `DEPLOYMENT.md`

## API маршруты

| Метод | Маршрут | Что делает |
|-------|---------|-----------|
| GET | `/cv` | Форма резюме |
| POST | `/cv/store` | Сохранить данные в сессию |
| GET | `/cv/preview` | Предпросмотр резюме |
| GET | `/cv/download` | Скачать PDF |
| GET | `/cv/list` | Посмотреть сохранённые версии |
| GET | `/cv/load/{id}` | Открыть сохранённую версию |

## Данные и безопасность

- **Не нужна регистрация** — всё сохраняется локально в сессии браузера
- **Сохраняй версии навсегда** — используй "Сохранить в библиотеку" для хранения в SQLite БД
- **Данные приватные** — мы ничего никуда не отправляем
- **Защита от атак** — встроенная защита Laravel: CSRF, валидация входных данных, XSS protection

Больше про безопасность: смотри `SECURITY.md`

## Решение проблем

### "Страница не загружается"
- Убедись что сервер запущен: `php -S localhost:8000 -t public`
- Проверь права доступа на папки `storage` и `database`

### "PDF не скачивается"
- Проверь что установлена `barryvdh/laravel-dompdf`
- Убедись что в браузере не заблокированы загрузки

### "Данные исчезли после обновления"
- Это нормально — данные формы хранятся в сессии
- Сохраняй важные резюме в библиотеку чтобы они не потерялись

## Структура проекта

```
cv-builder/
├── app/Http/Controllers/
│   └── CVController.php          # Основная логика
├── resources/views/cv/
│   ├── index.blade.php           # Форма резюме
│   ├── preview.blade.php         # Превью/печать
│   └── print.blade.php           # Экспорт PDF
├── routes/
│   └── web.php                   # Все маршруты
├── database/
│   └── database.sqlite           # БД сохранённых резюме (автоматически)
└── composer.json                 # Зависимости
```

## Технологический стек

- **Framework** — Laravel 11 (минималистичный, лёгкий)
- **Язык** — PHP 8.3
- **Экспорт PDF** — Dompdf (barryvdh/laravel-dompdf)
- **База данных** — SQLite
- **Frontend** — Vanilla JavaScript + CSS
- **Хостинг** — Docker + Render.com

## Лицензия

MIT License — используй свободно!

---

Создано для людей, которым нужен простой и рабочий конструктор резюме. Без рекламы, без трекинга, без ненужных фич.
